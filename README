## Reading Excel File in NodeJS and converting the same to JSON

**Clone the Repo and run below commands in terminal**
```
cd NodeExcel
bower install
node excel.js
```

All the excel Files data is displayed on Terminal
